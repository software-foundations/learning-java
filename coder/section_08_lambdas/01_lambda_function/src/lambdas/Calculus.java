package lambdas;

public interface Calculus {
	
	// public abstarct are implicit for all methods in a interface
	// public abstract double execute(double a, double b) {
	double execute(double a, double b);
}